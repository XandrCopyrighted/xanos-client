import { GridBox } from "std-widgets.slint";

export component WelcomePage inherits Window {

    GridBox {
           Text {text: "Working in";}
           width: 289px;
           height: 511px;
        }
    GridBox {
            Text {text: "Progress";}
            width: 305px;
            height: 410px;
        }
}